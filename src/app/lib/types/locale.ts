export type Dictionary = {
    companyName: string,
    components: {
        header: {
            title: string,
            github: string,
            google: string
        },
        footer: {
            libreTranslate: string
        }
    }
}