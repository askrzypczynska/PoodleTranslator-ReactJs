export type AppTheme = {
    colors: {
        primary: string,
        background: string,
        foreground: string,
        error: string,
        typography: string,
        input: string,
        border: string
    },
    media: {
        sm: number
    }
}